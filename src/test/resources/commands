String str ="aaabbddbad";

for(int i=0; i<str.length; i++)
{

count=0;
for(int j=0; j<str.length; j++)
{
if(ch[i]==ch[j])
{
count++;
}
}
system.out.println("The count of"+c[j]+"is"+count);
}

[15:10] Punugupati, Krishnaveni

how many times each character are repeated in the given string

String str ="aaabbddbad";

O/P = a4b3c0d3

[15:29] Punugupati, Krishnaveni

a b c

1 2 3

4 5 6

7 8 9

…or create a new repository on the command line
echo "# tet" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/SoniaBhavya/tet.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/SoniaBhavya/tet.git
git branch -M main
git push -u origin main

git pull origin main
git branch -d Test/Sonia